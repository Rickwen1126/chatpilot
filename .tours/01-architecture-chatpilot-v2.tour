{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "01 - Architecture: ChatPilot v2 Gateway Data Flow",
  "steps": [
    {
      "title": "Context",
      "description": "## 白話導讀\n\n這條 tour 其實是在看：**一則 LINE 群組訊息怎麼從 webhook 進來、經過層層關卡、最後變成 chatbot 回覆或異步任務結果推回去**。\n\n如果先記不住細節，先抓住每個 step 只是在回答「**這層做了什麼決策、不做什麼**」。\n\n## ChatPilot 是什麼\n\nChat-driven AI agent gateway。前端接 LINE（或其他平台），後端用 GitHub Copilot SDK 跑 LLM。核心設計是「即時聊天」與「異步任務」兩條分離的 data flow 共用同一個 Message Hub 入口。\n\n## 錨定已知\n\n如果你用過 **Express middleware chain**：ChatPilot 的 `Webhook → Hub → Router → Chatbot` 就是類似的 pipeline，只是每層不是 `next()` 而是 callback injection。如果你玩過 **Celery**：Task Scheduler + RunnerPool 就是簡化版的 broker + worker 模式，只是用 `asyncio.Queue` 替代 Redis。\n\n## 這條路徑在解決什麼問題\n\n**場景**：小團隊的 LINE 群組裡有個 AI bot，你 @bot 會即時回覆，也能叫它跑比較久的任務（查庫存、產報告），完成後自動推結果回群組。\n\n**技術挑戰**：chatbot 不能被長時間任務 block、群組閒聊要被收集成 context 而非觸發回覆、多個 chatbot 要根據 config 路由、平台 adapter 要可插拔。\n\n## 這條路徑展示什麼 mental model\n\n走完後你會建立 **Ports & Adapters + Hub-Spoke** 的 mental model：\n- **Ports**：Adapter Protocol 把平台差異封在邊界外\n- **Hub**：MessageHub 做唯一的進出閘門（mention filter + busy/idle gate）\n- **Spoke**：Router、Chatbot、Scheduler 是 Hub 的 callback，Hub 不知道它們的存在\n\n## 為什麼值得讀\n\n這個架構展示了怎麼用 **callback injection** 取代 import dependency 來做模組解耦。Hub 只知道 `on_proceed` callback，不 import router 或 chatbot。這個 pattern 比 event bus 簡單，比硬依賴靈活，是 Python async 服務常見的中間地帶。\n\n## 架構圖\n\n```\nLINE ──→ Webhook ──→ Adapter.parse() ──→ Hub.receive()\n                                           │\n                                    ┌──────┼──────────┐\n                                    │ mention?  busy?  │\n                                    │ /command?        │\n                                    └──────┬──────────┘\n                                           │\n                          ┌────────────────┼────────────────┐\n                          ▼                                 ▼\n                    on_proceed()                      context_buffer\n                          │\n              ┌───────────┼───────────┐\n              ▼                       ▼\n         Router.resolve()      on_command()\n              │                  /model, /chatbot\n              ▼\n     ChatbotManager.get_session()\n              │\n              ▼\n     ChatbotSession.send_message()\n              │\n              ├──→ SDK send_and_wait() ──→ Adapter.send_reply()\n              │\n              └──→ [tool call: submit_task]\n                        │\n                        ▼\n                  Scheduler.enqueue()\n                        │\n                        ▼\n                  RunnerPool._run()\n                        │\n                        ▼\n                  PipelineExecutor.execute()\n                        │\n                        ▼\n                  Hub.push() ──→ Adapter.push_message()\n```"
    },
    {
      "file": "src/chatpilot/server/webhook.py",
      "line": 17,
      "ref": "8fee49d7a8222c167e17d228663f7667e5fb73b8",
      "title": "1. Inbound: Webhook 薄層",
      "description": "## Insight\n\n**機制**：Webhook handler 是整個系統的 HTTP 入口，但它只做三件事：找 adapter、verify、parse → 丟給 hub。它不知道 chatbot、router、scheduler 的存在。\n\n**故障模式**：如果在這層做業務邏輯（比如直接呼叫 chatbot），LINE 的 webhook 會 timeout（LINE 要求 webhook 在幾秒內回 200）。把處理推到 `hub.receive()` 後面的 `asyncio.create_task` 才能非阻塞。\n\n**設計取捨**：用 `request.app.state` 存 adapters/hub 引用，而非 dependency injection。好處是零依賴設定；壞處是 type checker 不知道 state 的型別，IDE 自動補全失效。\n\n---\n\n這層其實就是一個 HTTP 轉譯器：把平台的 raw payload 轉成統一 Message 後交給 Hub。你在 LINE 群組發的每則訊息，最終都通過這個 `for msg in messages: await hub.receive(msg, adapter)` 迴圈進入系統。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：接收 webhook POST、用 adapter verify 簽章、parse 成 Message 列表、逐則送 hub。\n\n**為什麼這樣寫**：\n- 好處：adapter 和 hub 從 app.state 取，webhook 完全不 import 任何業務模組\n- 好處：verify 和 parse 分開，可以 fail fast（簽章錯直接 401）\n- 壞處：`request.app.state.adapters` 沒有 type annotation，pure dict access\n\n**思考問題**：\n1. 如果一次 webhook 包含 5 則 message，但第 3 則處理失敗，前 2 則和後 2 則會怎樣？\n2. LINE webhook 有 retry 機制，如果回 500 會重送。這個 handler 什麼情況下會回 500？\n3. 為什麼 verify 和 parse 不合併成一個 `adapter.handle_request()` 方法？"
    },
    {
      "file": "src/chatpilot/hub/hub.py",
      "line": 51,
      "ref": "8fee49d7a8222c167e17d228663f7667e5fb73b8",
      "title": "2. Hub: 中央閘門的決策樹",
      "description": "## Insight\n\n**機制**：`receive()` 是所有訊息的必經之路，它做一棵 3-branch 決策樹：\n1. `/command` 前綴 → 攔截給 `on_command` callback\n2. 非 mention（群組閒聊）→ 存 context buffer，不回應\n3. mention + idle → drain buffer、set busy、`asyncio.create_task` 送 `on_proceed`\n4. mention + busy → 存 buffer + 回「處理中」\n\nHub **不知道** router 和 chatbot 的存在，它只呼叫 callback。\n\n**故障模式**：L105 的 `asyncio.create_task` 沒有存 task reference。如果 Hub 物件被 GC，task 可能被一起回收。更重要的是，L81 的 busy 檢查和 L117 的 `set_busy` 之間有一個 race window：兩則快速連續的 @bot 訊息可能都看到 idle，都進入 `on_proceed`。在 asyncio 單線程模型下，只有 `await` 點會 yield，所以 L51-107 之間沒有 await（L64-78 的 buffer 操作是同步的），理論上不會 race。但 L94-97 的 `send_reply` 是 await，如果兩則訊息都走到 busy 分支，第二則的「處理中」回覆可能先到。\n\n**設計取捨**：用 callback injection（`on_proceed`/`on_command`）而非直接 import router。這讓 Hub 可以獨立測試，但代價是 wiring 延遲到 app factory，且 callback 簽名不直觀（需要看 type alias）。\n\n---\n\n把 Hub 想像成大樓的前台：它不處理任何業務，只決定「這個人該去哪間辦公室」和「現在有沒有人在忙」。群組閒聊就像有人在大廳聊天，前台默默記下來（context buffer），等有人指名找 bot 時才把聊天紀錄一起交給辦公室。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：訊息進入的 3-branch 判斷 + busy/idle gating + context buffer drain/inject。\n\n**為什麼這樣寫**：\n- 好處：Hub 零業務耦合，callback 換掉就能改行為\n- 好處：context buffer drain 確保不會重複注入（drain 後清空）\n- 壞處：`asyncio.create_task` 的 fire-and-forget 無法得知處理結果\n- 壞處：busy 狀態用 route_id 而非 chatbot_name，同一個 chatbot 服務兩個群組時各自獨立（可能是設計意圖也可能是 bug）\n\n**思考問題**：\n1. 如果要支持「一個 chatbot 同時處理多則訊息」（而非一次一則），Hub 的哪些地方需要改？\n2. `on_proceed` callback 裡如果 raise exception，busy 狀態會怎樣？（看 `_process_and_idle` 的 finally）\n3. context buffer 的 `format_context()` 格式是硬編碼的。如果不同 chatbot 需要不同的 context 格式，該在哪一層處理？\n\n## Review Notes\n\n**私聊 busy gate + buffer 的行為問題**：私聊時 `is_mention` 永遠 True，所以每則訊息都過 busy/idle gate。使用者連發兩則時，第二則存進 buffer 回「處理中」，但 idle 後 buffer 不會自動觸發處理——要等下一則訊息進來才 drain。結果是第二則變成第三則的 context prefix，而非獨立 request。在群組這合理（閒聊是 context），但在私聊裡使用者的每則訊息都是獨立 request，被當 context 處理不符合預期。需決定：(1) idle 後自動 re-trigger pending mentions (2) 私聊不走 busy gate (3) 接受現狀。"
    },
    {
      "file": "src/chatpilot/routing/router.py",
      "line": 28,
      "ref": "8fee49d7a8222c167e17d228663f7667e5fb73b8",
      "title": "3. Router: Config-Driven 路由競標",
      "description": "## Insight\n\n**機制**：Binding Router 是一個分數競標機制。每個 binding 定義 match 條件（platform、group_id、user_id），對每條進來的 message 算分，最高分者勝出。空 match = 預設 binding（score 0，永遠參賽但永遠最低分）。\n\n**故障模式**：（已修）原本 scoring 和 filtering 分散在 `calculate_score()` 和 `resolve()` 兩處，已合併成單一函式用 `NO_MATCH = -1` 做 hard filter。目前的風險是同分行為未定義——`score > best_score` 用 `>` 不是 `>=`，同分時 config 裡先出現的 binding 勝出，但這個行為沒有文件說明。使用者不知道 YAML 裡 binding 的排列順序會影響路由結果。\n\n**設計取捨**：分數表（`match_weights`）放在 YAML config 裡，可擴展新維度不改 code。代價是 router 必須用泛化的 `dict[str, str]` 做 match，失去 type safety。如果加了一個 config 維度但忘了在 `calculate_score` 加對應邏輯，新維度永遠拿不到分。\n\n---\n\n Router 就像一場無聲拍賣：每個 binding 出價（score），最高價得標（拿到這則訊息的處理權）。沒有任何 binding 出得了價的訊息就被丟棄。你在 routes.yaml 加一條 `match: {group_id: \"xxx\"}` 就是多了一位競標者。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：遍歷所有 bindings，計算每個的 match score，找最高分者，回傳 ChatRoute。\n\n**為什麼這樣寫**：\n- 好處：純函式計算，無副作用，好測試\n- 好處：支援 hot reload（`update()` 方法替換 bindings + weights）\n- 壞處：`_get_dimension`/`_get_weight` 是 if/elif chain，新增維度要改三處（YAML schema + 兩個函式）\n- 壞處：O(n) 遍歷所有 bindings，大量 bindings 時效能不佳（但 MVP 不是問題）\n\n**思考問題**：\n1. 如果兩個 binding 同分，目前的行為是什麼？（看 `score > best_score` 的 `>` vs `>=`）\n2. `calculate_score` 對「有 match 但沒匹配到任何維度」回傳 0，跟「空 match（default）」一樣是 0。Router 怎麼區分這兩種？\n3. 如果要加一個新 match 維度（比如 `user_role`），需要改哪幾個地方？\n\n## Review Notes\n\n**同分行為未定義且隱式依賴 YAML 順序**：`score > best_score` 用嚴格大於，同分時先出現的 binding 勝出。使用者調整 YAML 裡 binding 的排列順序就會改變路由結果，但沒有任何文件或 log 說明這個行為。應在 quickstart.md 或 routes.yaml 註解中明確說明，或改用 `>=` 讓後定義的優先（last-match-wins，類似 CSS specificity 同分時後者覆蓋）。"
    },
    {
      "file": "src/chatpilot/chatbot/session.py",
      "line": 32,
      "ref": "8fee49d7a8222c167e17d228663f7667e5fb73b8",
      "title": "4. Chatbot: SDK 的友善包裝",
      "description": "## Insight\n\n**機制**：ChatbotSession 做兩件事：(1) 把 context prefix 和 user message 串成一個 prompt（因為 SDK 只接受單一 `message: str`）、(2) 把 SDK 的各種 exception 翻譯成友善的中文回覆。它是 SDK 和業務邏輯之間的 **Anti-Corruption Layer**。\n\n**故障模式**：L52 catch 的是 Python 內建的 `TimeoutError`，不是 `chatpilot.core.errors.TimeoutError`。如果 SDK 的 `send_and_wait` raise 的是 `asyncio.TimeoutError`（它是 builtin TimeoutError 的子類），會被正確 catch。但如果 SDK raise 自己包裝的 timeout（不是 builtin 子類），就會掉進 L55 的 generic catch，回覆「系統暫時不可用」而非「處理超時」。\n\n**設計取捨**：context prefix 是字串串接（`f\"{context_prefix}\\n{text}\"`），不是結構化資料。好處是 SDK 不需要任何修改；壞處是 LLM 必須靠 system message 裡的指引來區分 context 和 direct message，格式脆弱。\n\n---\n\n ChatbotSession 就像一個翻譯：使用者說中文（text + context），它翻成 SDK 聽得懂的格式（一個 prompt string），SDK 回英文（raw text），它再翻成使用者看得懂的 Response。翻譯過程中如果 SDK 掛了，它不會讓錯誤直接噴給使用者，而是給一個友善的「暫時不可用」。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：組 prompt（context + text）、呼叫 SDK、處理錯誤。\n\n**為什麼這樣寫**：\n- 好處：錯誤不會 propagate 到上層，使用者永遠拿到可讀的回覆\n- 好處：timeout 和其他錯誤分開處理，日誌等級不同（warning vs error）\n- 壞處：catch Exception 太寬泛，可能吃掉 programming error\n\n**思考問題**：\n1. `send_and_wait` timeout 60s 是寫死的。如果某個 chatbot 需要更長的 timeout（比如它帶的 tool 會做 HTTP 呼叫），怎麼讓它可設定？\n2. 如果 SDK session crash（ProcessExitedError），這裡會 catch 進 generic exception。ChatbotManager 怎麼知道要重建 session？\n3. context prefix 串在 user message 前面，LLM 可能混淆兩者。有什麼更 robust 的做法？"
    },
    {
      "file": "src/chatpilot/tools/factory.py",
      "line": 35,
      "ref": "8fee49d7a8222c167e17d228663f7667e5fb73b8",
      "title": "5. ToolFactory: 存取控制邊界",
      "description": "## Insight\n\n**機制**：ToolFactory 是 tool 的中央註冊處，但它最重要的工作不是註冊——是**存取控制**。它有兩個產出方法：`get_tools_for_chatbot`（只給 global + chatbot_only）和 `get_tools_for_pipeline`（只給 global + agent_team_only）。Agent team tool（如 `submit_task`）被標記後，如果 pipeline agent 嘗試呼叫它，會直接 raise ValueError——這是防止**遞迴 task 提交**（chatbot assign task → pipeline 裡的 agent 又 assign task → 無限迴圈）的安全閥。\n\n**故障模式**：如果忘了呼叫 `mark_agent_team_tool()`，`submit_task` 就不會被標記，pipeline agent 就能呼叫它，造成 task 遞迴。這個標記是手動的，沒有自動偵測機制。\n\n**設計取捨**：用 `AccessLevel` IntEnum 做存取控制，而非 RBAC 或 capability-based。簡單直接，但新增存取級別（比如 `admin_only`）需要改 enum + 改兩個 filter 方法。更好的做法可能是用 set-based 允許清單，但 MVP 三級夠用。\n\n---\n\n ToolFactory 就像公司的鑰匙管理員：前台人員（chatbot）只能拿到公共區域和前台的鑰匙，倉庫工人（pipeline agent）只能拿到公共區域和倉庫的鑰匙，但任何人都不能拿到「派任務給別人」的鑰匙（agent team tool）——否則工人會無限轉包。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：根據 tool 名稱清單 + access level 過濾，回傳 SDK 格式的 tool 定義。\n\n**為什麼這樣寫**：\n- 好處：所有 tool 存取控制集中一處，不散落在各模組\n- 好處：遞迴防護明確（raise 而非默默忽略）\n- 壞處：`mark_agent_team_tool` 是手動操作，容易遺忘\n- 壞處：pipeline filter 的 `raise ValueError` 在生產環境會直接 crash 該 pipeline\n\n**思考問題**：\n1. `get_tools_for_chatbot` 裡未找到的 tool 只是 log warning + skip，但 `get_tools_for_pipeline` 裡遇到 agent team tool 卻 raise。為什麼待遇不同？\n2. 如果要支持「某些 chatbot 不能用 submit_task」，現在的 access level 設計能做到嗎？\n3. `_to_external_tool` 不包含 handler。SDK 怎麼知道 tool 被呼叫時要執行什麼？（提示：handler 在哪裡被使用？）\n\n## Review Notes\n\n**BUG: tool handler 斷路**：`_to_external_tool` 輸出 `{name, description, parameters}` 給 SDK，不含 handler。SDK 收到 LLM 的 tool call 後需要 callback 回我們的 code 執行 handler，但 SdkSession 沒有註冊 `on_tool_call` callback，`get_handler()` 也沒有任何 caller。整條 tool execution 鏈是斷的。需要研究 SDK 的 tool callback 機制後補接。\n\n**設計問題: `mark_agent_team_tool` 和 `AccessLevel` 是平行的兩套存取控制**：`submit_task` 同時標記為 `CHATBOT_ONLY`（access level filter）又標記為 agent team tool（遞迴防護），兩個 flag 做的事有重疊。應合併成單一 access level（例如新增 `AGENT_TEAM = 4`），讓 ToolDefinition 建立時就透過 type 鎖死存取級別，不需要額外手動呼叫 `mark_agent_team_tool()`。用 type contract 取代 runtime flag，避免忘記標記。"
    },
    {
      "file": "src/chatpilot/scheduler/runner.py",
      "line": 67,
      "ref": "8fee49d7a8222c167e17d228663f7667e5fb73b8",
      "title": "6. Runner: 異步任務的生命週期",
      "description": "## Insight\n\n**機制**：`_run()` 是 task 生命週期的核心：`queued → running → completed/failed`。它做四件事：(1) 標記 running + persist、(2) 執行 pipeline、(3) 根據結果更新 status + persist、(4) push 結果回原對話。注意 persist 做了兩次（開始一次、結束一次），確保 crash 後重啟可以看到「有個 task 卡在 running」。\n\n**故障模式**：L99 的 `await self._store.save(task)` 如果失敗（SQLite write error），task 狀態不會被持久化，但 push 仍然會執行（L107-110）。使用者看到「任務完成」但 DB 裡還是 running。反過來如果 push 失敗（L109），task 狀態已經正確持久化，只是使用者看不到通知——這是可接受的（send & forget 策略）。\n\n**設計取捨**：用 `asyncio.Semaphore` 做並行控制而非 worker pool。Semaphore 簡單但沒有 work stealing、priority、rate limiting。Runner 的 `_consume` loop 用 1 秒 timeout polling，不是 event-driven。好處是 graceful shutdown 簡單（`self._running = False`），壞處是有最多 1 秒的 idle latency。\n\n---\n\n RunnerPool 就像工廠的生產線：有 N 個工位（Semaphore），排隊的訂單（Queue）一個一個被領走處理。做完後把結果寫進帳本（SQLite），同時發通知給客戶（push）。帳本和通知是獨立的——帳本寫失敗不影響通知，通知發失敗不影響帳本。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：執行 pipeline、更新 task status、persist 到 SQLite、push 結果回 chat。\n\n**為什麼這樣寫**：\n- 好處：雙寫 persist（run 前 + run 後）讓 crash recovery 有跡可循\n- 好處：push 失敗不 raise，不影響後續 task 處理\n- 壞處：沒有 task timeout 機制，如果 pipeline 掛住，runner slot 永遠被佔用\n- 壞處：store.save 失敗會靜默吞掉（沒有 try-catch），實際上會 crash 整個 `_run`\n\n**思考問題**：\n1. 如果 `config.scheduler.task_timeout = 300`（5 分鐘），但 `_run()` 裡完全沒用到這個值。timeout 應該加在哪裡？\n2. `_consume()` 的 1 秒 timeout polling 意味著 shutdown 最慢要等 1 秒。如果用 `asyncio.Event` 做 signal，怎麼改？\n3. push 結果的 text 格式是硬編碼的中文。如果要支持多語系怎麼辦？"
    },
    {
      "file": "src/chatpilot/server/__init__.py",
      "line": 85,
      "ref": "8fee49d7a8222c167e17d228663f7667e5fb73b8",
      "title": "7. App Factory: Composition Root",
      "description": "## Insight\n\n**機制**：`lifespan()` 是整個系統的 **Composition Root**——所有元件在這裡被建立、接線、啟動。注意 Hub、Router、ChatbotManager 之間**沒有直接 import 依賴**。Hub 不知道 Router 的存在，是 `on_proceed` closure（L90-102）把它們串起來。這個 closure 捕獲了 `binding_router` 和 `chatbot_manager`，形成一個隱式的 dependency graph。\n\n**故障模式**：L116 的 `config.chatbots` 引用的是啟動時載入的 config 物件。但 `on_config_reload`（L164-167）更新了 `chatbot_manager` 的 configs，卻沒有更新 `on_command` closure 裡捕獲的 `config` 變數。所以 `/chatbot general-bot` 指令在 hot reload 加入新 chatbot 後**可能會錯誤拒絕**——因為 closure 看到的仍然是舊 config。\n\n**設計取捨**：用 closure 做 wiring 而非 DI container。好處是零框架依賴、一目了然；壞處是 closure capture 的 stale reference 很難被靜態分析工具發現，是經典的 long-lived closure bug。\n\n---\n\n App factory 就像劇場的舞台總監：它把所有演員（adapter、hub、router、chatbot、scheduler）安排到位，然後說「開演」。演員之間不直接說話，而是通過預先排練好的 cue（callback）互動。總監（lifespan）在散場時負責所有人的清理。\n\n---\n\n## Code Detail\n\n**這段在幹嘛**：初始化所有元件、用 closure 接線、啟動 scheduler runner、設定 config hot reload。\n\n**為什麼這樣寫**：\n- 好處：所有 wiring 在一個函式裡，一眼看完系統全貌\n- 好處：lifespan context manager 自動處理 shutdown 順序\n- 壞處：`on_command` closure capture stale `config`，hot reload 後行為不一致\n- 壞處：lifespan 函式太長（160 行），兼做 init + wiring + error handling\n\n**思考問題**：\n1. 如果要修 stale config bug，最小改動是什麼？（提示：`on_command` 裡的 `config.chatbots` 換成什麼？）\n2. shutdown 順序是 runner → store → observer → chatbot → sdk。如果順序反了（先關 sdk 再關 chatbot），會發生什麼？\n3. 如果要讓這個系統支持 graceful restart（不丟失進行中的 task），需要在 lifespan 加什麼邏輯？"
    }
  ]
}
