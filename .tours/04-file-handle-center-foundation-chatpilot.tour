{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "04 - File Handle Center Foundation: ChatPilot File Governance",
  "steps": [
    {
      "title": "Context",
      "description": "## 白話導讀\n\n這條 tour 其實是在看：ChatPilot 怎麼替 file/media 建立第一個正式治理中心，讓檔案不再只是 scattered local side effect，而是 route-scoped external memory 的起點。\n\n如果先記不住細節，先抓住每個 step 只是在回答「這個 foundation 如何把平台來源、系統主鍵、DB 納管與 local asset lifecycle 接起來」。\n\n## ChatPilot FileHandleCenter 是什麼\n\n它是 ChatPilot 裡新的 file ingress / governance boundary。不是單純 downloader，而是把 adapter 送進來的 source descriptor 轉成系統內 canonical file identity，並負責 local materialization、retention 與後續可治理的資產生命週期。\n\n## 錨定已知\n\n如果你熟悉 MessageHub / adapter pattern，這裡其實是在做同樣的事：adapter 把平台差異翻進統一契約，中心服務再根據這個契約執行後續流程。只是這次承接的是 file，而不是 message。\n\n## 這條路徑在解決什麼問題\n\n原本 repo 的 file/media parser、adapter lookup、download 與 local path 使用散在多處。這讓 vision、STT、document flow 都各自維護一套 file logic，也使未來 tool/agent/generated file 無法被有序納管。\n\n## 這條路徑展示什麼 mental model\n\n走完後應該建立的模型是：\n- source handle 是平台翻譯後的來源描述\n- file_id 是系統內 opaque primary key\n- FileHandleCenter 是 ingest + persistence + materialization orchestration\n- DB 與 local layout 一起形成 file external memory 的基底\n\n## 為什麼值得讀\n\n讀完這條 tour，你會理解 ChatPilot 如何在不一次重寫所有 tool 的前提下，先把 file governance 的骨架立起來，之後再逐段把 hub、STT、vision、document 與 generated files 接上去。\n\n## DeepWiki 延伸閱讀\n\n先讀 `docs/file-handle-center/spec.md` 與 `docs/file-handle-center/plan.md`，再回頭看這條 code tour，會更容易對應規格與實作邊界。"
    },
    {
      "file": "src/chatpilot/files/models.py",
      "line": 47,
      "ref": "2bee56f138b0658fcf76ece9fa4aeecbc3e208a5",
      "title": "Step 1: Source Handle 與 Materialized Asset 正式分層",
      "description": "## Insight\n\n**機制**：這段把 file world 切成兩層：`SourceHandleInput` 描述來源檔案怎麼被 adapter 找回，`CanonicalFileHandle` 加上系統分配的 `file_id`，而 `MaterializedAsset` 則描述下載/落地後的資產狀態。這讓 source identity、system identity 與 local persistence 不再混在一起。\n\n**故障模式**：如果這三層不分開，之後很容易再次把 `platform/native id`、`local path`、`download result` 混成同一個 identifier，route migration、retention、share/export 與 generated-file lineage 都會變得難以控制。\n\n**設計取捨**：v1 把 `native_locator` 保持為 `str`，是刻意的 YAGNI。好處是先用目前 LINE 足夠穩的 source truth 建立基礎模型；代價是未來若第二個平台需要複合 locator，必須升級 schema，但這比現在過度抽象更穩。\n\n## 白話解析\n\n這段在整條鏈上扮演「先把檔案世界的名詞講清楚」的角色。它其實就是在確保系統談的是三件不同的東西：平台來源、系統主鍵、以及本地化後的資產結果。之後如果某條流程壞掉，你比較容易判斷是 source 翻譯錯、center 註冊錯，還是 local asset lifecycle 出問題。\n\n## Code Detail\n\n**這段在幹嘛**：\n\n定義 `FileKind`、`FetchStatus`、`StorageBackend`、`ScanStatus`、`RetentionClass`，以及 `SourceHandleInput`、`CanonicalFileHandle`、`SourceFetchResult`、`MaterializedAsset` 四個核心模型。\n\n**為什麼這樣寫**：\n\n好處：\n- `CanonicalFileHandle.from_source(...)` 把 system identity 分配收斂到單一入口。\n- `MaterializedAsset` 只描述已落地/已暴露的結果，不污染 source 層。\n- validator 把空字串這種低階壞資料提早擋掉。\n\n壞處：\n- 目前 models 還沒有 relation/note 對應型別，store 層暫時以 dict 在走。\n- `native_locator` 先用字串，之後要升級時需要一輪 schema migration。\n\n**思考問題**：\n- 為什麼 `route_id` 要留在 handle 裡，而不是只靠 `(platform, native_locator)`？\n- 如果未來 generated file 沒有平台來源，還能否沿用這組模型？\n- `MaterializedAsset` 什麼時候應該擴充成多 backend / derived asset graph？"
    },
    {
      "file": "src/chatpilot/files/store.py",
      "line": 28,
      "ref": "2bee56f138b0658fcf76ece9fa4aeecbc3e208a5",
      "title": "Step 2: SQLite 單表起步，但 relation/note 已先留治理掛點",
      "description": "## Insight\n\n**機制**：這段把 file governance 的最小 DB 版圖定成三張表：`file_assets` 是 file unit 本體，`file_relations` 承接複雜關係，`file_notes` 承接可長留的文字脈絡。它刻意不從 type-specific tables 起步，而是把複雜度導向 relation。\n\n**故障模式**：如果只把檔案 bytes 落到 local path、不做 DB 納管，之後 session reset、cross-agent handoff、generated file lineage、retention cleanup 都只能靠檔名規則和 path 猜測，context 很快就會爆炸。\n\n**設計取捨**：這裡先用 `file_assets` 單表混合 source identity 與目前 materialized state，是刻意選的 v1 tradeoff。好處是落地快、查詢直覺；代價是等未來 multi-backend materialization 或大量 derived assets 出現時，再拆表會是一個明確的下一步。\n\n## 白話解析\n\n這段在整條鏈上扮演「把 file 從路邊檔案變成可治理資料」的角色。它其實就是在說：即使原始檔之後被回收，系統也要知道它曾經屬於哪條 route、跟哪些 task/tool 有關，以及代理人曾留下哪些摘要或分析。你之後看到的效果，就是 agent 不再只會說『請重傳』。\n\n## Code Detail\n\n**這段在幹嘛**：\n\n建立 `file_assets`、`file_relations`、`file_notes` schema，並提供 CRUD、relation append、note append、expired asset query 與 row→model 的轉換 helper。\n\n**為什麼這樣寫**：\n\n好處：\n- `route_id` 直接放在 `file_assets`，先把 ownership / partition 簡單定下來。\n- relation/note 先上極簡版，之後 generated file / transcript / annotation 有路可走。\n- `TimeService` 被收進 store，避免這層再長出平行時間來源。\n\n壞處：\n- v1 還沒有 dedupe / source upsert 的正式策略。\n- `file_relations` / `file_notes` 目前先回 dict，不如 model 化來得強型別。\n\n**思考問題**：\n- 未來若同一 source file 需要共享到多 route，應優先調整哪一層：`file_assets.route_id` 還是 relation table？\n- `file_notes` 何時該分成 analysis/transcript 專表，何時維持 generic note table 更有彈性？\n- local metadata 已進 DB 後，還需要 `meta.json` 的角色是什麼？"
    },
    {
      "file": "src/chatpilot/files/center.py",
      "line": 56,
      "ref": "2bee56f138b0658fcf76ece9fa4aeecbc3e208a5",
      "title": "Step 3: Center 把 register、download、prefetch、ensure_local 收到單一 orchestration shell",
      "description": "## Insight\n\n**機制**：這段把 file lifecycle 的主要 orchestration 收回 `FileHandleCenter`：`register` 分配 `file_id`，`download_now` 透過 adapter fetch source 並落地 local asset，`prefetch` 開 background task，`ensure_local` 則提供更高階的 caller 意圖 API。這讓 sync/async 變成 center 的使用模式，而不是 adapter contract 的分裂。\n\n**故障模式**：如果這層不存在，adapter、hub、tool 會再次各自決定何時下載、存哪裡、怎麼回 adapter 找檔，最後只會回到現在散落 parser / local hardcode path 的局面。`\n\n**設計取捨**：這版 center 先選擇最小可運作：background prefetch 先用 in-memory `asyncio.Task`，cleanup 先刪 local asset 但保留記憶。好處是 foundation 很快站穩；代價是之後要把 prefetch 接入正式 task scheduler，以及補 route lookup/note API，仍有下一輪工作。\n\n## 白話解析\n\n這段在整條鏈上扮演「真正替 file 做事的人」的角色。它其實就是在把 adapter 拿回來的 source bytes，正式變成系統可用的 local asset，順手把 retention、meta 與 local dir 一起納管。你之後會在 STT、vision、document_edit 這些流程看到它的效果，因為這些流程終於可以不再自己管平台與路徑。\n\n## Code Detail\n\n**這段在幹嘛**：\n\n實作 `register`、`download_now`、`prefetch`、`get_asset`、`ensure_local`、`cleanup_expired` 與 `_persist_local_asset`。\n\n**為什麼這樣寫**：\n\n好處：\n- `register()` 保持純 canonicalization，沒有偷跑 side-channel。\n- `download_now()` 明確把 adapter 只當 fetch primitive，用 store + layout 管後面的資產治理。\n- `prefetch()` 先有錯誤 logging，避免 background 任務失敗完全靜默。\n\n壞處：\n- 目前 `prefetch()` 還只是 in-memory task，不具備 durable queue 或 resume 能力。\n- `register()` 尚未做 source lookup/upsert，重複註冊策略仍待下一階段定義。\n\n**思考問題**：\n- 什麼時候 `prefetch()` 應該升級成 scheduler 任務，而不是 event-loop task？\n- 哪些 caller 應該只拿 `get_asset()`，哪些應該直接走 `ensure_local()`？\n- `cleanup_expired()` 目前只刪 asset、不刪記憶，這如何支撐 route-level file recall？"
    },
    {
      "file": "src/chatpilot/adapters/protocol.py",
      "line": 41,
      "ref": "2bee56f138b0658fcf76ece9fa4aeecbc3e208a5",
      "title": "Step 4: Adapter contract 被擴充，但責任仍卡在平台邊界",
      "description": "## Insight\n\n**機制**：這段沒有把 file center 變成另一個 platform adapter，而是把現有 `ChannelAdapter` contract 往 file/source translation 多延伸半步：新增 `build_source_handle(...)` 與 `fetch_source_file(...)`。這讓 adapter 繼續扮演平台翻譯器與 fetch primitive 提供者，而非 orchestration owner。\n\n**故障模式**：如果 adapter 不提供這個契約，後續 hub/tool 很容易又回去自己組 source descriptor、自己判平台差異，結果 file governance 會在真正接入時再次破洞。\n\n**設計取捨**：目前先保留舊 `download_media(...)` 相容路徑，而不是一次刪掉。好處是 foundation 可以先落地且不炸既有 tool；代價是現在同時存在 legacy 與 new contract，直到後續 tool convergence 完成前，心智模型會稍微重疊。\n\n## 白話解析\n\n這段在整條鏈上扮演「把 file 也納進既有 adapter pattern」的角色。它其實就是在說：平台差異還是由 adapter 吸收，但系統內部以後只想看統一的 source handle 與 fetch result。你實際會在未來 line parser/hub ingress 的嫁接時看到它的效果。\n\n## Code Detail\n\n**這段在幹嘛**：\n\n擴充 `ChannelAdapter` protocol，加進 `build_source_handle(...)` 和 `fetch_source_file(...)` 兩個 file-related contract。\n\n**為什麼這樣寫**：\n\n好處：\n- 直接沿用目前成功的 adapter pattern，不另發明一套 file adapter 系統。\n- `fetch_source_file(...)` 與 `download_media(...)` 可先平行存在，便於漸進重構。\n- 後續 mock/line/cli adapter 都能用同一介面接上 center。\n\n壞處：\n- protocol 現在同時承載 message 與 file concerns，抽象寬度比之前更大。\n- `kind` 先以 `str` 進 protocol，型別約束主要交給 Pydantic model 吃。\n\n**思考問題**：\n- 未來 generated file 沒有平台來源時，是否還需要由 adapter 參與？\n- 當 `download_media(...)` 最終退役時，哪些 tool 會是最難收斂的？"
    },
    {
      "file": "src/chatpilot/server/__init__.py",
      "line": 355,
      "ref": "2bee56f138b0658fcf76ece9fa4aeecbc3e208a5",
      "title": "Step 5: Startup 先把中心接進 app state，之後才能沿流程逐段嫁接",
      "description": "## Insight\n\n**機制**：這段把 `SqliteFileStore` 與 `FileHandleCenter` 納進 app startup lifecycle，讓新中心從一開始就是正式 runtime service，而不是某個 tool 私下 new 出來的 helper。這是之後 hub ingress、tool convergence、vision/STT POC 能沿既有 DI 路線嫁接的前提。\n\n**故障模式**：如果 center 只在局部工具裡被動建立，最後就會出現多個 store/path root/policy 來源，file governance 會再次退化成多頭馬車。\n\n**設計取捨**：這一步先只做 startup wiring，不急著馬上在 hub/tool 內大規模接入。好處是先讓 service container 站穩、測試可先做 foundation；代價是目前 `file_handle_center` 雖然存在，但真正被主流程使用還要下一個 milestone 才開始。\n\n## 白話解析\n\n這段在整條鏈上扮演「把新中心正式升格成系統服務」的角色。它其實就是在替下一個 milestone 鋪路：現在先把 store 和 center 放進 app state，後面 hub/tool 才能不用另開 side path 接它。你現在還看不到最終功能，但之後每一條 file ingress 都會從這裡受益。\n\n## Code Detail\n\n**這段在幹嘛**：\n\n在 startup 中初始化 `files.db`、建立 `FileHandleCenter`，掛到 `app.state.file_store` 與 `app.state.file_handle_center`，並在 shutdown 關閉 file store。\n\n**為什麼這樣寫**：\n\n好處：\n- 讓新中心直接沿用既有 startup/shutdown lifecycle。\n- adapter dict 用同一份引用，config reload 後 center 不需要重建整張 adapter map。\n- 後續 tool/hub 取用方式統一，便於 dependency wiring。\n\n壞處：\n- 在真正嫁接進 hub/tool 之前，這個 service 目前還屬於基礎設施成本。\n- 若未來 `prefetch` 接進 scheduler，startup 還要再多一層 wiring。\n\n**思考問題**：\n- hub ingress 應該直接從 `app.state.file_handle_center` 取用，還是透過更明確的 service injection？\n- 當未來 E2E 開始依賴 file center，測試 app lifecycle 需要補哪些 fixture？"
    }
  ]
}
