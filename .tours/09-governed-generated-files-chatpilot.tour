{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "09 - Governed Generated Files: ChatPilot FileHandleCenter",
  "steps": [
    {
      "title": "Context",
      "description": "## 這條 tour 其實是在看\n\n這條 tour 其實是在看：tool 產出的檔案，怎麼從『只是流程中的副作用』，變成 `FileHandleCenter` 底下正式受治理的 canonical file。\n\n如果先記不住細節，先抓住每個 step 只是在回答「當 tool 產出新檔時，系統要怎麼讓它立即進入 file lineage，而不是再次掉回 workpath 世界？」\n\n## ChatPilot 是什麼\n\nChatPilot 不只是會調工具的 chatbot，而是正往『有限度自治、可持續運作的 agent runtime』前進。這代表 tool output 不能再是一次性副作用，而必須變成可追蹤資產。\n\n## 錨定已知\n\n如果你熟悉 `register_local_file()` 那條線，可以把這一輪看成它的下一步：不是只有既存本機檔案能被納管，連記憶體中的 tool output bytes 也能直接成為 canonical file。\n\n## 這條路徑在解決什麼問題\n\n像 `document_edit` 這類工具會產出新的 Excel / DOCX。若它只回一個 URL 或 workpath，route recall、lineage、後續再處理都會斷。這條路徑是在把 output 直接接回中心邊界。\n\n## 這條路徑展示什麼 mental model\n\n走完後應該能建立：tool generated files 不是 exception，而是 file ingress 的另一種正式來源；generated file governance 與 external file governance 應該長在同一套中心模型上。\n\n## 為什麼值得讀\n\n因為這是 ChatPilot 從『能用工具』走到『能管理工具產物』的關鍵一步，也是外部檔案記憶真正開始可持續化的地方。"
    },
    {
      "file": "src/chatpilot/files/center.py",
      "line": 115,
      "ref": "4804be2d7813a5d622f6dedd19a8f5601e9e239e",
      "title": "register_bytes_file 把記憶體輸出變成 canonical file",
      "description": "## Insight\n\n**機制**：`register_bytes_file()` 讓 tool / pipeline 不需要先把 output 硬寫到某個工作目錄，再反過來匯入。它直接把 memory bytes 轉成 internal source handle、落地成 governed asset，再補 lineage relations。這讓『產生檔案』本身就成為中心服務的正式能力。\n\n**故障模式**：如果 generated output 仍只能先落到 workpath，再靠人工或額外 tool 匯回中心，系統會很快重新長出大量逃逸路徑，route recall 與 cleanup 也會變得不可信。\n\n**設計取捨**：這裡採用 `platform=\"internal\" + origin_type` 來標示 generated/local import 類來源。好處是能用現有 canonical handle 模型吸收新 ingress；代價是 internal source 目前仍不可 re-fetch，之後若要更強 rehydrate 能力，還得再補 source provider 模型。\n\n## 白話解析\n\n這段在整條鏈上扮演的是『把 tool output 直接變成系統資產』的角色。它其實就是在做一件很關鍵的事：檔案還沒來得及掉進 workpath 混亂之前，就先給它一個正式 `file_id`、正式 local asset、正式 lineage。之後如果這裡沒做好，generated file governance 只會停留在口號。\n\n## Code Detail\n\n這段在幹嘛：\n- 驗證 bytes 非空。\n- 建立 internal canonical handle。\n- 直接把 bytes 落地進 governed storage。\n- 更新主表 materialized state。\n- 補 `derived_from` / `generated_by_tool` / `generated_by_pipeline` relations。\n\n為什麼這樣寫：\n- 好處：tool output 可以直接進 file center，不必先經過暫存 workpath。\n- 好處：local import 與 generated bytes 最終共用同一條 internal asset path。\n- 壞處：internal source 仍是單向 materialization，失去 local asset 後還不能重新抓回。\n\n思考問題：\n1. 哪些 tool 最適合下一批導入 `register_bytes_file()`？\n2. internal source 若未來也要可 rehydrate，應該先補哪一種 provider/backup 模型？"
    },
    {
      "file": "src/chatpilot/tools/builtin/document_edit.py",
      "line": 224,
      "ref": "4804be2d7813a5d622f6dedd19a8f5601e9e239e",
      "title": "document_edit 現在會把輸出正式納管",
      "description": "## Insight\n\n**機制**：`document_edit` 不再只是把 output 上傳到 R2 然後回一條 URL，而是先把 edited bytes 用 `register_bytes_file()` 納入 `FileHandleCenter`，若有原始 canonical source 還會補 `derived_from` relation，最後再做 R2 upload 與回應文字。\n\n**故障模式**：如果 document tool 只回 URL，不回 canonical file，使用者之後再問『剛剛整理的 Excel 幫我再補一欄』時，系統仍然沒有真正的 file lineage，只能重新走平台檔或 workpath hardcode。\n\n**設計取捨**：這裡刻意讓 upload 與 governance 分開，先治理再曝光。好處是就算 upload 失敗，系統仍知道這個 generated file 存在；代價是 response 文案與後續 consumer 還要學會認得 `file_id`，不能只看 URL。\n\n## 白話解析\n\n這段在整條鏈上扮演的是『第一個真正把 tool output 接回中心』的角色。它其實就是在把 document_edit 從『加工完給你一個下載連結』，升級成『加工完後，這個新檔已經是 route 裡的一個正式資產』。這一步做對了，之後 Excel/Docx 的跨輪次再利用才會順。\n\n## Code Detail\n\n這段在幹嘛：\n- 保留原本編輯與 R2 upload 行為。\n- 在 upload 前，把 output bytes register 成 generated file。\n- 若有 source_handle，就補 `derived_from`。\n- 在回應文字中回傳 `file_id`。\n\n為什麼這樣寫：\n- 好處：tool output 不再只是外部 URL，而有內部 canonical identity。\n- 好處：後續要 route recall、再編輯、再分享都接得住。\n- 壞處：現在仍依賴 session context 才能決定 route ownership；無 context 的非標準調用還沒被完全治理。\n\n思考問題：\n1. 哪些使用者可見工具之後也應回傳 `file_id` 而不只回 URL？\n2. `generated_handle` 該不該在 UI/對話中有更正式的暴露格式，而不是只藏在文字裡？"
    },
    {
      "file": "src/chatpilot/tools/builtin/show_image.py",
      "line": 56,
      "ref": "4804be2d7813a5d622f6dedd19a8f5601e9e239e",
      "title": "show_image 先做 center-first 遷移，但不偷渡安全政策",
      "description": "## Insight\n\n**機制**：`show_image` 現在也和其他 consumer 一樣，會先根據 session context 與 legacy ref 回中心找 canonical file，命中後用 `ensure_local()` 取資產；只有 miss 時才 fallback 到舊 adapter download。這把它拉回同一條 file-access pattern。\n\n**故障模式**：如果 show_image 仍維持直接下載平台媒體，它就完全無法利用 canonical file 的 route scope、retention 與後續 relation-based exposure policy，最後仍是獨立旁路。\n\n**設計取捨**：這裡刻意只做 center-first migration，沒有順手把 relation-driven exposure gate 一起偷渡。好處是風險小、責任線清楚；代價是安全判斷還只是『先回中心再處理』，還沒升級到 lineage-aware controlled exposure。\n\n## 白話解析\n\n這段在整條鏈上扮演的是『把主動回推圖片這件事也先收回中心』的角色。它其實就是在先把 data path 接對，而不假裝安全政策已經設計完成。這種克制很重要，因為 show_image 牽涉的不只是檔案取用，還有之後更大的系統安全邊界議題。\n\n## Code Detail\n\n這段在幹嘛：\n- 保留 url 直推模式。\n- ref 模式先 route-scoped lookup canonical file。\n- 命中後讀 local asset，否則 fallback legacy adapter。\n- 最後上傳 R2 並 inject 回應。\n\n為什麼這樣寫：\n- 好處：show_image 也開始共用 file center 主路徑。\n- 好處：不會因為還沒設計好 relation gate，就卡住整條遷移。\n- 壞處：relation-driven exposure policy 仍是後續必要工作，這一輪只先保證 data path 正確。\n\n思考問題：\n1. relation-driven exposure policy 最小要看哪幾種 relation 才夠用？\n2. show_image 之後是否應該優先接受 `file_id` 而不是 ref？"
    },
    {
      "file": "tests/unit/test_document_edit.py",
      "line": 325,
      "ref": "4804be2d7813a5d622f6dedd19a8f5601e9e239e",
      "title": "測試把 generated file governance 鎖成真正 invariant",
      "description": "## Insight\n\n**機制**：新增測試不是只驗 document_edit 還會成功，而是直接驗證 output 會被納管成新的 `file_id`，並帶著 `derived_from` 與 `generated_by_tool` relation。這把『tool output 應受治理』從設計意圖變成可回歸檢查的系統 invariant。\n\n**故障模式**：如果只驗 URL 回來、檔案內容正確，那之後很容易再次回退成『功能有做出來，但治理鏈沒有接上』。這正是 agent 系統在變大後最危險的假成功。\n\n**設計取捨**：測試目前仍是 targeted，不是 full E2E。好處是能準確打在 governance invariant；代價是 relation-driven exposure、跨 route reuse 等更大場景還需要後續整合驗證。\n\n## 白話解析\n\n這段在整條鏈上扮演的是『把 generated file 不准逃逸這件事寫成測試規矩』。它其實就是在防止我們未來維護時又偷偷覺得「回個 URL 就好」。有了這個測試，tool output 治理至少在這條主線上不容易退化。\n\n## Code Detail\n\n這段在幹嘛：\n- 先建原始 canonical source file。\n- 呼叫 document_edit。\n- 從結果中取回 generated `file_id`。\n- 驗證 local asset 與 relations 都存在。\n\n為什麼這樣寫：\n- 好處：直接驗證 generated file 是否真的進中心，而不是只停在 response text。\n- 好處：未來類似 tool 可以照同一種測法收斂。\n- 壞處：目前仍透過 response text 取 `file_id`，等正式暴露格式定下來後還可以再收斂。\n\n思考問題：\n1. generated `file_id` 之後是否值得有統一的 message/response exposure 格式？\n2. 哪些下一個 tool 最需要沿用這個 invariants-driven test pattern？"
    }
  ]
}
