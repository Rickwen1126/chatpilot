# 2026-03-31 TODO

## Multi-Line Route Key Migration

### 決策

- multi-line 上線後，系統全面以新的 named route key 為準
  - 例如 `line:shinyipaint:C...`
- 舊的 legacy key 不作為持續相容層
  - 例如 `line:C...`
- 舊 key 對應的舊資料先保留
  - 視為歷史資料 / 測試資料 / fallback 參考
- 對重要資料採用 **copy-based migration**
  - 複製到新 key
  - 不直接刪除舊 key

### 問題定義

- 這不是單純的 adapter bootstrap 過渡問題
- 真正的風險是 canonical route key 從 `line:C...` 變成 `line:{channel}:C...` 後，
  系統會把同一個真實 LINE 對話拆成兩個不同 target
- 影響範圍包含所有以 `route_id` 為 key 的資料：
  - session context metadata
  - route labels
  - memory store 內的 route-scoped records
  - reminder / schedule
  - task history
  - custom prompts / observer / permission 相關資料

### 待辦

- 盤點 shinyipaint 相關、目前仍掛在 legacy `line:*` key 底下的重要資料
- 列出需要 migration 的資料類型與來源檔 / store
- 定義 legacy key → named key 的對應規則
  - `line:C...` → `line:shinyipaint:C...`
  - `line:U...` → `line:shinyipaint:U...`
- 先做 copy，不做 move
  - 新 key 建立後保留舊資料
  - 避免一次性切換造成資料遺失或回滾困難
- 優先處理 shinyipaint 真正會影響線上行為的資料
  - labels
  - reminder / schedule
  - custom prompts
  - observer / memory 類資料
  - task history（若有營運價值）
- 補 migration 驗證
  - 同一個真實對話在 named key 下可讀到複製後的重要資料
  - 舊 key 保留但不再作為主要寫入目標

### 初步盤點結果（依目前本機 `data/`）

#### A. 需要處理的實體檔 / store

- `data/chatpilot.db`
  - `memory_memos`
  - `memory_custom_prompts`
  - `memory_reminders`
  - `memory_schedules`
  - `memory_observations`
  - `trigger_keywords`
- `data/tasks.db`
  - `tasks`
- `data/route_labels.json`
- `data/session_contexts/`
  - 目前 worktree 只有測試資料，但正式 migration 時要一起納入盤點

#### B. 目前本機已存在的 legacy LINE 資料

- `memory_memos`
  - 2 筆 / 1 個 `line:*` route
- `memory_custom_prompts`
  - 6 筆 / 3 個 `line:*` routes
- `memory_reminders`
  - 6 筆 / 4 個 `line:*` routes
- `memory_schedules`
  - 目前沒有 `line:*` 資料
- `memory_observations`
  - 3 筆 / 1 個 `line:*` route
- `trigger_keywords`
  - 2 筆 / 2 個 `line:*` routes
- `tasks`
  - 有多筆 `chat_route_id LIKE 'line:%'` 的歷史任務
- `route_labels.json`
  - 目前看到的 labels 全部仍是 legacy `line:*` key

#### C. 已觀察到的可疑 / 需要特別處理的 key

- `tasks.db` 出現：
  - `line:U...-shinyipaint`
- `chatpilot.db / memory_reminders` 出現：
  - `line:C...-shinyipaint`
  - `line:U...-shinyipaint`

這些看起來不像新的 named route key（正確應為 `line:shinyipaint:C...`），
比較像舊 bug / 舊過渡邏輯留下來的 malformed key。

migration 時應把它們視為獨立清單，不要直接套用一般 `line:C... -> line:shinyipaint:C...` 規則。

#### D. 目前 migration 優先順序

1. `route_labels.json`
2. `memory_custom_prompts`
3. `memory_reminders`
4. `memory_schedules`
5. `memory_observations`
6. `memory_memos`
7. `trigger_keywords`
8. `tasks.db` 的 `tasks`
9. `data/session_contexts/` 與 workspace sidecars

#### E. 下一步盤點任務

- 確認 shinyipaint 實際對應的 legacy routes 清單
- 區分三種 key 類型：
  - 正常 legacy：`line:C...` / `line:U...`
  - 正常 named：`line:shinyipaint:C...` / `line:shinyipaint:U...`
  - malformed：`line:...-shinyipaint`
- 為每一種資料來源定義 copy 規則
- 決定 `tasks.db` 是否只複製仍有營運價值的 task history，而不是全部搬運

### 文件 / Review 補充

- `01 - Multi-Line Adapter` codetour 應補充：
  - 風險不只是在 `_init_adapters()` 的 runtime key lookup
  - 更深層的是 route keyspace migration 可能造成 identity split
  - 若不處理 migration，`SessionContext` 與所有 route-scoped state 都會把舊 / 新 key 視為不同 target

## Session Identity 設計債

### 問題定義

- 目前 `sdk_session_id` 仍然由 `route_id + chatbot_name` 組成
- 這在短期內是可接受的 shortcut，但長期會遇到同一類問題：
  - route migration 時 session continuity 被切斷
  - chatbot rename / route rename 直接改變 runtime identity
  - delegated async / 多節點時，derived identifier 被誤當成真正 identity

### 目前判斷

- `SessionContextRegistry` 以 `sdk_session_id` 為 key 這件事本身沒有問題
- 真正有風險的是：
  - `sdk_session_id` 的生成策略目前仍是 derived composition
- 這和 file 設計上「source fields 不該直接當 primary key」是同一類壓力

### 暫時結論

- 現在先不重做 session identity
- 目前 `sdk_session_id = route_id + chatbot_name` 仍可接受，因為：
  - local-first 單機模型還撐得住
  - `SessionContextRegistry` 已經把 parse 依賴降下來
- 但這應被視為 **明確的設計債**，不是長期最終方案

### 未來方向

- 升級為：
  - opaque `sdk_session_id`
  - `(route_id, chatbot_name) -> sdk_session_id` 的正式 binding / mapping
- 讓 route identity 與 runtime session identity 脫鉤
- 這樣 route migration、chatbot rename、session continuity 才能分開處理

### 觸發重構的條件

- 需要保留 route migration 前後的 session continuity
- 需要跨機器 / 多節點共享 session control plane
- 需要讓 chatbot rename / route rename 不影響 SDK resume identity
- 需要更明確區分：
  - runtime actor identity
  - target route identity

### 待辦

- 把目前 `sdk_session_id` 的使用面盤點清楚
  - SDK resume
  - workspace path
  - response injector
  - session context metadata
- 評估如果改成 opaque session id，需要新增哪些 binding store / migration
- 未來若開始做 file handle / file asset DB，可一起對齊：
  - internal opaque primary key
  - domain/source fields 作為 unique lookup，而不是 primary key
