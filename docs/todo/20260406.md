# 2026-04-06 TODO

## Observer vNext 實作里程碑

本輪已完成 observer vNext core implementation：

- `route_group` / `observation_profiles` schema 上線
- `binding.reply_policy` 與 `binding.processing_policy` 上線
- `binding.observation.capture/consume` 上線
- 舊 `observer_mode` / `observer_allowed_consumers` 正式移除
- `query_observations` 改為 `group` canonical API
- Hub 改成 canonical inbound message fan-out：
  - reply lane
  - observation lane
- observation lane 使用獨立 buffer
- observation batch summarize 明確走獨立 worker session
- `addressed + capture` 已成立

## 驗證結果

- `uv run ruff check src/ tests/` → 綠
- `uv run pytest tests/` → `199 passed`
- `bash tests/e2e/run_e2e.sh` → `52 passed / 0 failed`

新增的 self-contained E2E 覆蓋：

- `never + none + capture`
- `addressed + interactive + capture`
- `query_observations(group=...)`

## 後續

- 需要補 milestone codetour / review artifact
- 下一步可正式進 observer vNext review，再決定是否直接進下一輪 refactor（如 discovery onboarding / `shadow`）
