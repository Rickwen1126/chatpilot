---
name: speckit.analyze
description: Perform a non-destructive cross-artifact consistency and quality analysis across spec.md, plan.md, and tasks.md.
---

Use this repo-local skill when the user asks for `speckit.analyze` or the equivalent Claude command.

1. Read `../../../.claude/commands/speckit.analyze.md`.
2. Follow the workflow there as the source of truth.
3. Keep the operation read-only unless the user explicitly asks for follow-up edits.
4. Translate Claude handoff wording into Codex equivalents only when suggesting next steps.
